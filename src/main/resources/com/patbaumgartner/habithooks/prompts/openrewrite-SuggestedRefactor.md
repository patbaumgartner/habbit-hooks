OpenRewrite produced an automated refactoring patch for this file. This is a high-signal modernization hint: it means a codemod recipe found a concrete cleanup or migration opportunity.

**How to fix it:**

1. Open the generated patch and inspect the proposed change in context.
2. Apply the change if it is behavior-preserving for your codebase.
3. Re-run tests and habit-hooks to confirm no regressions.

Treat OpenRewrite output as a fast lane for safe, repetitive refactors that agents should not hand-edit from scratch.