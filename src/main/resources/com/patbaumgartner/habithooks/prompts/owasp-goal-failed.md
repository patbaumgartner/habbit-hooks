# OWASP Dependency Check Failed

The vulnerability scan failed before producing parseable findings. The project does not have trustworthy local CVE feedback yet.

Run the Dependency Check goal directly, inspect network/cache/plugin errors, and restore JSON report generation before treating the dependency set as clean.
