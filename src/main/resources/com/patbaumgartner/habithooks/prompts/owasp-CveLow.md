# Low CVE

A low CVE is still useful supply-chain feedback. Fix it when a safe dependency update is available, especially if the dependency is already being touched.

Do not churn major versions only for low-risk findings unless the update is already planned and tested.
